{
  "scripts": {
    "deploy": "yarn run travis-deploy-once 'yarn run semantic-release'",
    "semantic-release": "semantic-release",
    "travis-deploy-once": "travis-deploy-once"
  },
  "devDependencies": {
    "semantic-release": "^24.2.2",
    "travis-deploy-once": "^5.0.11"
  }
}
